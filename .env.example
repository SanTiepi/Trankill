# Port d'écoute du serveur HTTP (défaut : 3500)
PORT=3500

# Note : PORT est la SEULE variable d'environnement utilisée par le code
# (process.env.PORT dans src/server.mjs). Aucune clé API, aucun secret —
# la stack est entièrement locale et zéro-deps.
